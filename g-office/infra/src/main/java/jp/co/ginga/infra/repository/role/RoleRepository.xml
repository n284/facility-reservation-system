<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="jp.co.ginga.infra.repository.role.RoleRepository">
	<resultMap id="role"
		type="jp.co.ginga.infra.repository.role.RoleEntity">
		<id property="roleId" column="role_id" />
		<result property="roleName" column="role_name" />
	</resultMap>
	<select id="findAll" resultMap="role">
		select
		role_id
		,role_name

		from
		m_role
		;

	</select>


	<select id="findByRoleId" resultMap="role">
		select
		role_id
		,role_name

		from
		m_role

		where
		role_id=#{roleId}
		;
	</select>

	<insert id="insert">
		insert
		into
		m_role
		(
		role_id
		,role_name
		)values(
		#{roleId}
		,#{roleName}
		)
		;

	</insert>

	<update id="update">
		update m_role
		set
		role_name=#{roleName}
		where role_id=#{roleId}
		;

	</update>


	<delete id="delete">
	delete from
	m_role
	where
	role_id=#{roleId}
	;



	</delete>



</mapper>